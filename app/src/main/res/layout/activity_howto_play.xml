<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.yourdomain.physicalsnakegame.HowtoPlayActivity">

    <!-- Adds scrolling to the how to play menu -->

    <ScrollView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/scrollView"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true" >

        <!-- adds the how to play text to the how to play menu, and makes the text scrollable -->
        <TextView
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:textAppearance="?android:attr/textAppearanceLarge"
            android:text="@string/HowToPlayText"
            android:id="@+id/textView2"
            android:lineSpacingMultiplier="1.2"
            android:textSize="28sp"
            android:isScrollContainer="true"
            android:scrollbars="vertical"
            android:fillViewport="true"/>
    </ScrollView>

</RelativeLayout>
